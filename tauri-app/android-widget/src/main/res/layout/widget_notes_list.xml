<?xml version="1.0" encoding="utf-8"?>
<!-- 2b — recent notes (4x2). Header row plus a ListView fed by
     NotesRemoteViewsService. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_bar_bg"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:paddingTop="12dp"
    android:paddingEnd="10dp"
    android:paddingBottom="8dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:letterSpacing="0.08"
            android:maxLines="1"
            android:text="@string/widget_notes_label"
            android:textColor="@color/widget_muted"
            android:textSize="9.5sp"
            android:textStyle="bold" />

        <ImageView
            android:id="@+id/widget_notes_new"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:background="@drawable/widget_action_bg"
            android:contentDescription="@string/widget_notes_new_widget_label"
            android:padding="6dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_plus" />
    </LinearLayout>

    <ListView
        android:id="@+id/widget_notes_items"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="6dp"
        android:layout_weight="1"
        android:divider="@null"
        android:dividerHeight="0dp" />

    <TextView
        android:id="@+id/widget_notes_empty"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:text="@string/widget_notes_empty"
        android:textColor="@color/widget_muted"
        android:textSize="12sp" />
</LinearLayout>
