<?xml version="1.0" encoding="utf-8"?>
<!-- No values-night twin: the sheet's colours come from widget_colors.xml,
     which already has one, so the theme itself never changes. -->
<resources>
    <style name="Theme.QuickCapture" parent="@android:style/Theme.Translucent.NoTitleBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
    </style>
</resources>
