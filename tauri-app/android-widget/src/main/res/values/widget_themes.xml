<?xml version="1.0" encoding="utf-8"?>
<!-- No values-night twin: the colours come from widget_colors.xml, which already
     has one, and the configuration dialog's parent is DayNight, so neither theme
     itself changes with the night mode. -->
<resources>
    <style name="Theme.QuickCapture" parent="@android:style/Theme.Translucent.NoTitleBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
    </style>

    <!-- The template button's configuration (TemplateButtonConfigureActivity).
         A floating window, so Android 15's enforced edge-to-edge never reaches
         it. The accent is the widgets' own, not the platform's teal. -->
    <style name="Theme.TemplateButtonConfigure" parent="Theme.MaterialComponents.DayNight.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@drawable/quick_capture_sheet_bg</item>
        <item name="android:windowMinWidthMajor">60%</item>
        <item name="android:windowMinWidthMinor">90%</item>
        <item name="colorAccent">@color/widget_accent</item>
        <item name="colorSecondary">@color/widget_accent</item>
        <item name="android:colorAccent">@color/widget_accent</item>
        <item name="android:colorControlActivated">@color/widget_accent</item>
    </style>
</resources>
